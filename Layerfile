#This is an example webapp.io configuration for NodeJS
FROM vm/ubuntu:18.04
# To note: Layerfiles create VMs, *not* containers!

# Install node
RUN curl -sL https://deb.nodesource.com/setup_12.x | bash && \
    apt install nodejs && \
    rm -f /etc/apt/sources.list.d/nodesource.list
RUN npm install -g http-server

COPY . .
SECRET ENV countries_secrets
RUN echo $countries_secrets | base64 -d > ~/.env
RUN npm install 

RUN BACKGROUND npm run start
EXPOSE WEBSITE http://localhost:7890

